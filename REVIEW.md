# 專案 Review 紀錄

日期：2026-09-06。已整理本機檔案，未變更線上 Supabase。

## 已修正

| 問題 | 處理 |
| --- | --- |
| 快速切店可能顯示舊菜單 | 請求序號阻擋過期回應，切換立即清空選項，飲料與加料平行載入 |
| 重複價格／杯數／訊息／按鈕程式 | 集中到 shared.js，保留 price 舊資料相容 |
| 從 DOM data-price 取得單價 | 改從已讀取的菜單資料計價，送出前驗證店家與品項配對 |
| 送出途中改表單與延遲清空 | 送出時暫停編輯，成功即重設，失敗保留輸入；成功畫面使用送出時的店家 |
| 管理頁統計只取得單次 API 上限 | 分頁取得所有訂單並依 ID 對應店家 |
| 特殊品名導致分組錯誤 | 用 Map 分組，支援 constructor、__proto__ |
| 給店家的複製文字遺漏備註 | 將備註納入摘要與規格分組 |
| RLS 零筆更新／刪除被誤認成功 | 驗證回傳 ID 或刪除數量 |
| 刪店先刪飲料，可能僅完成一半 | 只送店家刪除，依資料庫既有 cascade 保持原子性 |
| 管理頁刷新選擇跳回第一家／殘留舊品項 | 保留有效店家選擇，無店家或讀取失敗清空舊畫面 |
| 登入失敗與操作例外 | 共用 finally 恢復按鈕，登入改 form 支援 Enter |
| CSS 重複與長店名重疊 | 合併杯數／hover／時間規則，移除未使用 row、導覽樣式與多餘 important，長文字換行 |
| 文件／SQL 不符合目前程式 | 更新文件，補菜單欄位 migration 與管理操作政策；新建 SQL 不再開放匿名讀訂單 |

移除了未使用 category_id 查詢、登入回傳 data 變數、延遲跳轉與清空計時器，以及大量無意義換行。
未刪線上欄位、歷史訂單或任何現有功能。

## 驗證

- shared.js、app.js、admin.js、login.js、tests/review.cjs 通過 Node 語法檢查。
- Chrome headless 執行 tests/review.cjs：15 項檢查通過。
- 覆蓋價格與杯數、送出內容／重設／失敗、快速切店、1105 筆統計、備註與特殊名稱、
  零筆異動、新增品項、單筆／全部刪單、店家操作、登入／重新導向與手機版排版。
- 桌面及手機截圖已人工檢視。
- 所有遠端請求皆由測試攔截，Supabase 使用模擬資料；未以測試操作真實訂單。
- PostgreSQL／RLS 未實際執行驗證，模擬測試不能證明線上權限正確。

## 部署與尚待處理

1. 發布網站時需一併上傳 shared.js 與更新的 HTML／JS／CSS。
2. 若線上缺菜單欄位，執行 supabase_migrations.sql；管理政策依 SECURITY_SETUP.md 設定。
3. 所有 Auth 帳號仍視為管理者。角色分離尚未實作，不能直接開放一般使用者註冊。
4. 訂單單價仍由前端送入；SQL 只驗基本乘算一致性，資料庫依菜單驗價尚未實作。
5. 已有訂單引用的店家／飲料仍受外鍵保護，刪除可能失敗；可停用以保留歷史。
6. 備份 .review-backup 與測試截圖 .review-artifacts 僅供本機使用，不需部署。

原始檔備份：.review-backup/20260906-014136。


## 後續清理
依使用者要求，測試程式、截圖與本機 SQL 檔（含備份中的 SQL）已移除。上文保留作為歷史工作紀錄，相關檔名不代表目前仍有提供。線上資料庫未變更。
