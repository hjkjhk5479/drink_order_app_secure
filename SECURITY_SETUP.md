# 管理頁安全設定

首頁提供管理者登入連結；資料安全仍由 Supabase 權限控制。

目前沿用「所有 Auth 帳號皆為管理者」的設計，沒有獨立管理員角色。
請關閉公開註冊，只手動建立可信任的管理者帳號。

## 預期權限

| 操作 | 未登入 | 已登入管理者 |
| --- | --- | --- |
| 讀取菜單 | 可 | 可 |
| 送出訂單 | 可 | 可 |
| 讀取／刪除訂單 | 不可 | 可 |
| 新增／啟停／刪除店家與飲料 | 不可 | 可 |

在 Supabase 確認 stores、products、toppings、orders 的 RLS 已啟用，
並檢查現有政策是否符合上述權限。
本機 SQL 檔已移除，不代表線上政策已變更或已驗證。

## 尚待處理

- 資料庫依菜單驗價、檢查店家與飲料配對。
- 管理者角色分離與匿名濫發訂單防護。
- 發布後用未登入及登入帳號分別驗證權限。

前端僅使用 publishable／anon 公開金鑰，不可放 service_role 金鑰。